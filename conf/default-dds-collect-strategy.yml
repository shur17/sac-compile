sac:
  collectStrategies:
    - name: "DDS_DATABASE_META"
      enable: true
      interval: 30000

    - name: "DDS_DATABASE_CAPACITY"
      enable: true
      interval: 60000

    - name: "DDS_COLLECTION_META"
      enable: true
      interval: 30000

    - name: "DDS_COLLECTION_CAPACITY"
      enable: true
      interval: 60000

    - name: "DDS_SHARD_STATS"
      enable: true
      interval: 10000

    - name: "DDS_SERVICE_META"
      enable: true
      interval: 30000

    - name: "DDS_NODE_META"
      enable: true
      interval: 30000

    - name: "DDS_NODE"
      enable: true
      interval: 10000

    - name: "DDS_NODE_HEALTH"
      enable: true
      interval: 5000

    - name: "DDS_QUERY"
      enable: true
      interval: 5000
        
    - name: "DDS_NODE_MONITOR"
      enable: true
      interval: 60000

    - name: "DDS_SERVICE_MONITOR"
      enable: true
      interval: 60000

    - name: "DDS_PERF_NODE_LOG"
      enable: true
      interval: 300000