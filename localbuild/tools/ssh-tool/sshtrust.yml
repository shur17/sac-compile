# sshtrust.yml

# 控制主机，远程主机需要信任的主机
controlHost: "192.168.31.81"

# 远程主机列表
# 密码是可选的配置项，可以通过 --ssh-passwd 指定一个全局的密码
# 当主机没有配置密码时，将使用 --ssh-passwd 指定的全局密码
# 当配置文件没有配置密码，也没有使用 --ssh-passwd 指定全局密码时，将通过交互获取
remoteHosts:
  - "192.168.17.70"
